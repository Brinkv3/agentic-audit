# --- Core pipeline ---
llm-adapter[anthropic] @ git+https://github.com/Brinkv3/llm-adapter.git  # Provider-agnostic LLM client
pydantic               # Typed inter-agent state models
openpyxl               # Excel workbook generation
PyMuPDF                # PDF parsing (import as fitz)
python-docx            # DOCX parsing
pandas                 # Structured data handling
tiktoken               # Token counting

# --- Dev / test ---
pytest                 # Test harness
